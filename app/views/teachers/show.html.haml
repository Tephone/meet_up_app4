%h2 マイページ
%p= @teacher.name
%p= @teacher.email
%p= i18n_language_name(@teacher.language.name)
%p= @teacher.profile
%p= image_tag @teacher.image.url if @teacher.image.present?
= link_to '編集', edit_teacher_registration_path
